---
interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<h2
  id={id}
  class="text-ink font-mono text-xl font-semibold tracking-tight sm:text-2xl"
>
  <span aria-hidden="true" class="text-accent mr-2">##</span><slot />
</h2>
